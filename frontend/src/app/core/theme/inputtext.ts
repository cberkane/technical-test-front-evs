import { ComponentsDesignTokens } from '@primeuix/themes/types';
import { primary, error } from './_variables';

export const inputtext: ComponentsDesignTokens['inputtext'] = {
  root: {
    focusBorderColor: primary,
    invalidBorderColor: error,
  },
};