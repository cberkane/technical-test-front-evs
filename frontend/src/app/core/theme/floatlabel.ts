import { ComponentsDesignTokens } from '@primeuix/themes/types';
import { primary } from './_variables';

export const floatlabel: ComponentsDesignTokens['floatlabel'] = {
  root: {
    focusColor: primary,
  },
};

